require 'rails_helper'

RSpec.feature "CommentsFeatures", type: :feature do
  # scenario "go to /articles without logged will redirect to /login" do
  #   visit '/articles'
  #
  #   expect(current_path).to eq('/login')
  # end
end
